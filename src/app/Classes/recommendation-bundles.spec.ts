import { RecommendationBundles } from './recommendation-bundles';

describe('RecommendationBundles', () => {
  it('should create an instance', () => {
    expect(new RecommendationBundles()).toBeTruthy();
  });
});
